# Quick Start

Maintainer note: internal implementation/release artifact; not a public readiness claim.


For most readers, start with the root [README.md](../README.md).

Contributor setup:

```bash
npm install
npm run install:sidecar
```

Development:

```bash
npm run dev:sidecar
npm run dev:desktop
```

If you are preparing the repo for public GitHub, use [PRE_UPLOAD_CHECKLIST.md](./PRE_UPLOAD_CHECKLIST.md).

Most other files in `docs/` are maintainer notes or historical release artifacts.
